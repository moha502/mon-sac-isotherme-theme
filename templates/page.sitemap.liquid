<div class="content">
    <div class="section-header-template">
      <h1 class="mb-none">{{ page.title }}</h1>
    </div>
    <div class="rte">
      {{ page.content }}
    </div>
    {% section 'sitemap' %}
</div>